{
  "name": "techgroup_dapr_db",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "new-migration": "prisma migrate dev --name academy_scheduler",
    "migrate": "npx prisma generate && npx prisma migrate deploy && echo Migrations completed successfully!"
  },
  "devDependencies": {
    "prisma": "^4.15.0"
  }
}
